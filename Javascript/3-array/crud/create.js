// Khởi tạo một mảng rỗng
let arr1 = [];

// Khởi tạo mảng với các phần tử
let arr2 = [1, 2, 3, 4, 5];

// Khởi tạo một mảng rỗng
let arr3 = new Array();

// Khởi tạo mảng với các phần tử
let arr4 = new Array(1, 2, 3, 4, 5);
console.log(arr4);

// Tạo một mảng với 5 phần tử, tất cả đều là undefined
let length = 5;
let arr = new Array(length);
